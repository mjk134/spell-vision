rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId}/messages/{messageId} {
      allow read, write: if true; // Allow all for signaling messages
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}